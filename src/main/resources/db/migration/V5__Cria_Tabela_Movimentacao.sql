CREATE TABLE Movimentacao (id SERIAL PRIMARY KEY,tipo VARCHAR(50) NOT NULL,horaInicio TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,horaFim TIMESTAMP WITH TIME ZONE,conteiner_id INTEGER, CONSTRAINT fk_conteiner FOREIGN KEY (conteiner_id) REFERENCES conteineres(id));